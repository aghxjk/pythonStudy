1. chapter01.py   数据结构和算法
    1.01 deque双向队列
    1.02 heapq队列实现算法
    1.03 列表堆化处理(heapq.heapify)
    1.04 查找最大或最小的N个元素(heapq.nsmallest, heapq.nlarges)
    1.05 优先级队列
    1.06 字典中的键映射多个值(defaultdict)
    1.07 字典排序(OrderDict)
    1.08 删除序列中相同元素并保持顺序
    1.09 命名切片(slice)
    1.10 序列中出现次数最多的元素(collections.Counter)
    1.11 通过某个关键字排序一个字典(operator.itemgetter)
    1.12 对象排序(operator.attrgetter)
    1.13 按字段将记录分组(itertools.groupby)
    1.14 过滤序列元素(filter)

2. chapter02.py    字符串和文本
    2.01 使用多个界定符分割字符串(str.endswith or str.startswith)
    2.02 用shell通配符匹配字符串(fnmatch)
    2.03 字符串替换(re.sub)
    2.04 字符串最短匹配、多行匹配、正则使用、删除无用字符、字符串对其、字符串合并、
         变量替换与格式化、指定列宽格式化、处理html和xml、字节字符串操作等详见《python cookbook(第3版)》

3. chapter03.py    数字日期和时间
    3.01 数字的四舍五入
    3.02

